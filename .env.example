# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins. Set to * for all origins.
CORS_ORIGIN=http://localhost:3000

# Database Configuration (Postgres)
# Connection URL: postgres://user:password@host:port/database
# For local dev without Docker, host is usually localhost
DATABASE_URL=postgres://walter:white@localhost:5432/boilerplate

# Postgres Container Configuration (for Docker)
POSTGRES_USER=walter
POSTGRES_PASSWORD=white
POSTGRES_DB=boilerplate

# JWT Configuration
JWT_SECRET=dfdhhf8gh523reh6qedn37dferpoawdn381j
JWT_EXPIRATION=15m
